B#include "holberton.h"

/**
 * _strlen - returns the length of a string.
 * @s : s is words
 * Return: quantiti is i
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
}

return (i);
}
