#include "holberton.h"
/**
 * puts2 - returns a string rev.
 * @str: s is words
 * Return: quantiti is i
 */
void puts2(char *str)
{
int i;
int len = _strlen(str);
for (i = 0; i <= len - 1; i += 2)
_putchar(str[i]);
_putchar('\n');
}
